#!/bin/sh

# trap 'pkill -P $$' EXIT

# wol 88:b1:11:e2:46:90
# wol a4:4c:c8:13:75:57

# echo "Please wait..."
# sleep 10
# ping -c 5 parrotos.lan

# ssh parrotos sudo /usr/local/bin/wake
# ssh -f -N -q -L5901:localhost:5900 parrotos &

# sleep 4
# vncviewer localhost:5901

# ssh parrotos sudo /usr/local/bin/suspend

# ping -c 10 parrotos.lan

export HACKENV_CORES=4
export HACKENV_MEMORY=4194304
notify-send.sh "ParrotOS" "Starting in about 1min..." -i "address-book-new" -o "GUI:hackenv --type=parrot gui --fullscreen" -o "Shutdown:hackenv --type=parrot down" -o "SSH:kitty hackenv --type=parrot ssh"
hackenv --type=parrot up && hackenv --type=parrot gui --fullscreen
