#!/bin/sh

API_KEY=$(pass pinboard_api_key)
DIR=~/.pinboard

FILE=`date +"%Y%m%d"`-pinboard-backup.json

URL="https://api.pinboard.in/v1/posts/all?auth_token=${API_KEY}&format=json"

cd $DIR
wget $URL -O $FILE
