#!/bin/sh

if usbctl check; then
    text="off"
    tooltip="USB protected"
    class="off"
else
    text="on"
    tooltip="USB unprotected"
    class="on"
fi

printf '{"text": "'"$text"'", "tooltip": "'"$tooltip"'", "class": "'"$class"'" }'
